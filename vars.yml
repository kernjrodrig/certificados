---
# Lista de clusters de OpenShift a gestionar
# Puedes definir múltiples clusters en esta lista
clusters:
  - name: DR_OCP

    api_url: "https://api.cluster-pvvpb.dynamic.redhatworkshops.io:6443"

    token: "sha256~jj85ib0NKjWFXAZtvWwoPMFma26ZFh0LQ7i6NnNgras"

    
    secret_name: "cert-manager-ingress-cert"

    
    namespace_ingress: "openshift-ingress"

    # COMENTADO: Ya no se usa - el certificado se lee directamente del secreto sin guardar en archivo
    # cert_file: "{{ role_path }}/files/cluster1-ingress-cert.crt"

    cert_file_crt: "{{ role_path }}/files/apps_dr-ocp_imss_gob_mx.crt"    # REQUERIDO (para reemplazo): Ruta al archivo del certificado del servidor (.crt)

    
    ca_intermedia_file: "{{ role_path }}/files/CA_Intermedia.crt"

    
    ca_raiz_file: "{{ role_path }}/files/CA_Raiz.crt"

    
    cert_file_combined: "{{ role_path }}/files/cert-combined.crt"

    
    key_file_new: "{{ role_path }}/files/apps_dr-ocp_imss_gob_mx.key"

    configmap_name_prefix: "custom-ca-dr-ocp"

    secret_name_prefix: "router-certs-dr-ocp"

    # COMENTADO: Ya no se usa - el proceso de backup fue eliminado del role
    # backup_file: "{{ role_path }}/files/ingress-backup-cluster1.yaml"

    namespace_ingress_operator: openshift-ingress-operator

    ingress_controller_name: default


# ============================================================================
# CONFIGURACIÓN DE CERTIFICADOS
# ============================================================================
certificate:
  # Días antes de la expiración para reemplazar automáticamente el certificado
  # Si el certificado expira en menos de este número de días, se ejecutará el reemplazo
  replacement_threshold_days: 60
  # Valor recomendado: 60 días (permite tiempo suficiente para renovación antes de expiración)

  